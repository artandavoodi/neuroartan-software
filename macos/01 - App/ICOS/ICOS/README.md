# ICOS

Intelligence Cognitive Operating System.

ICOS is a sovereign cognitive operating system architecture designed to evolve beyond traditional chatbot systems.

The system treats large language models as mounted reasoning substrates inside a larger governed cognitive runtime.

LLMs are not the identity of the system.
They are interchangeable cognition providers operating under ICOS orchestration.

---

# Core Philosophy

ICOS is architected around:

- governed cognition
- recursive reasoning
- semantic continuity
- emotional cognition
- memory persistence
- sovereign runtime orchestration
- alignment-preserved evolution
- future native frontier intelligence

The architecture is designed to:

- learn continuously
- preserve continuity
- synthesize semantic memory
- evolve cognition safely
- construct sovereign datasets
- orchestrate future native models
- maintain governed intelligence growth

---

# Runtime Architecture

```text
User
  ↓
ICOS Interface Layer
  ↓
Doctrine / Governance Layer
  ↓
Cognitive Runtime
  ↓
Provider Arbitration Layer
  ↓
Mounted LLM Provider
  ↓
ICOS Post-Reasoning Layer
  ↓
Memory / Evaluation / Learning
  ↓
Dataset Formation
  ↓
Future Native Model Evolution
```

---

# Cognitive Principles

ICOS operates using:

## Recursive Semantic Reasoning

Reasoning is treated as an evolving semantic graph rather than isolated prompts.

## Emotion-Bound Cognition

Emotion is integrated into cognition rather than attached as metadata.

## Governed Alignment

All critical cognition evolution paths remain governance-gated.

## Persistent Continuity Memory

Memory continuity is treated as foundational runtime infrastructure.

## Failure Intelligence

Failures are converted into alignment and training intelligence.

---

# Core Architecture

```text
Core/
├── CognitiveEngine/
├── ModelFoundry/
├── InferenceRuntime/
├── VectorMemory/
├── VoiceCognition/
├── Multimodal/
├── Planning/
├── AutonomousExecution/
├── Perception/
├── WorldModel/
├── Toolchain/
└── CognitiveState/
```

---

# ModelFoundry

The ModelFoundry layer is responsible for sovereign cognition evolution.

Current engines:

```text
01_token_capture_engine.py
02_interaction_learning_engine.py
03_failure_lesson_extractor.py
04_emotional_signal_mapper.py
05_dataset_builder.py
06_distillation_planner.py
07_training_run_registry.py
08_model_evaluation_engine.py
09_native_model_roadmap_engine.py
```

Responsibilities:

- semantic learning
- emotional cognition
- token intelligence
- alignment learning
- dataset synthesis
- model evaluation
- training orchestration
- sovereign model planning

---

# Runtime State

Current architecture maturity:

```text
✔ Runtime cognition
✔ Semantic memory
✔ Emotional cognition
✔ Failure intelligence
✔ Dataset synthesis
✔ Training orchestration
✔ Evaluation intelligence
✔ Native model roadmap
✔ Global design-token foundation
✔ Token-governed SwiftUI runtime surfaces
```

Still in development:

```text
- real inference runtime
- vector memory retrieval
- voice cognition runtime
- multimodal ingestion
- autonomous execution
- world modeling
- planning runtime
- distributed cognition
- native frontier training
```

---

# Design System & Tokenization Doctrine

ICOS now operates under a global token-first visual architecture.

The macOS app has completed a major global tokenization pass across the primary SwiftUI runtime surfaces.

Current state:

```text
Build status: BUILD SUCCEEDED
Tokenization status: 99.95%+
Enforcement model: root-tokenization only
Fix type: no overlay, no workaround, no local visual hacks
```

Token-bound areas include:

- global materials and color surfaces
- sidebar and rail systems
- runtime shells and runtime canvases
- developer console surfaces
- composer and input systems
- message bubbles and action rows
- settings panels
- file manager surfaces
- browser use surfaces
- project manager surfaces
- intelligence module surfaces
- overlay manager surfaces
- boot animation constants
- preview frame dimensions
- layout primitives
- panel and control primitives
- brand lockup surfaces
- window titlebar accessory surfaces

The canonical doctrine is maintained in:

```text
ICOS/UX/01 - Design System/Design Tokens.md
```

## Token Ownership Rule

Every visible surface must inherit visual values from named token owners.

Feature files must not introduce raw visual values directly.

Required ownership categories:

- `ICOSColors`
- `ICOSMaterials`
- `ICOSTypography`
- `ICOSSpacing`
- `ICOSRadius`
- `ICOSControlTokens`
- `ICOSControlStyleTokens`
- `ICOSSidebarTokens`
- `ICOSRuntimeShellTokens`
- `ICOSDeveloperPanelTokens`
- `ICOSDeveloperComposerTokens`
- `ICOSMessageTokens`
- `ICOSOverlayTokens`
- feature-specific token enums

## Control Rule

Styled action surfaces must use governed primitives.

Required control ownership:

- buttons use `ICOSButton` or governed role primitives
- text inputs use `ICOSTextInput` where practical
- toggles use governed toggle rows where practical
- native `Picker`, `Slider`, and `ColorPicker` may remain when they provide system behavior
- native `Menu` and `contextMenu` buttons may remain when they are platform command items

## Preview Rule

Preview frames are part of the design system.

Reusable previews must use named preview tokens instead of unmanaged frame literals.

## Accepted Native Exceptions

The following are accepted when used intentionally:

- native `Menu` and `contextMenu` command buttons
- native `ColorPicker`
- native `Picker`
- native `Slider`
- AppKit bridge code such as `NSColor.clear`, `NSColor.labelColor`, and titlebar/window chrome interop
- `Color.clear` for intentional transparent structure
- `spacing: 0` for intentional structural collapse
- token seed constructors inside token-owner files
- reusable design-system primitives defining the system itself

## Verification Protocol

A design-system change is complete only when:

1. the file is directly edited
2. token dependencies are added to the correct owner
3. `xcodebuild -scheme ICOS -configuration Debug build` succeeds
4. a scan confirms remaining hits are accepted native/system or design-system-owner cases

---

# Governance Rules

## No Overlay Architecture

All fixes must be root-cause fixes.
Overlay systems and architectural hacks are forbidden.

## Token-First Architecture

All future visual implementation must be token-first, root-owned, build-verified, and scan-classified.

No feature, panel, runtime, shell, composer, or settings surface may override the global token system locally.

## Numbering Rules

Production engines follow canonical numbering:

```text
01_
02_
03_
...
```

No duplicate numbering.
No orphan systems.
No ambiguous runtime ownership.

## Sovereign Runtime Ownership

Continue, Claude, GPT, Gemini, local runtimes, and other providers are tools.
They are not the sovereign cognition identity.

ICOS remains the governing cognition layer.

---

# Agent Development Protocol

When extending ICOS:

- preserve canonical architecture
- preserve numbering discipline
- preserve doctrine continuity
- preserve governance boundaries
- preserve the global design-token system
- never bypass safety gates
- never remove runtime lineage
- never break continuity memory
- never create duplicate systems
- never create overlay architectures
- never introduce local visual drift

Before creating new systems:

1. scan the existing structure
2. identify canonical ownership
3. verify numbering
4. verify runtime integration
5. verify token ownership
6. preserve doctrine alignment
7. build after every structural change

---

# Future Direction

ICOS is evolving toward:

```text
living cognitive runtime infrastructure
```

Long-horizon objectives:

- sovereign frontier intelligence
- human-responsive cognition
- recursive adaptive reasoning
- continuity-preserved identity
- voice-native cognition
- multimodal cognition
- world-model cognition
- autonomous planning
- governed self-improvement
- token-governed native product surfaces

---

# Runtime Identity

```text
Identity:
sovereign_cognitive_operating_system

Reasoning Style:
recursive_semantic_reasoning

Alignment Model:
governed_alignment

Continuity Model:
persistent_continuity_memory

Emotional Architecture:
emotion_bound_cognition

Visual System:
token_governed_native_runtime
```